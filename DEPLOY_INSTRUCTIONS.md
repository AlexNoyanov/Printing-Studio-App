# Инструкция по деплою на print-electrozavodskaya

## ✅ Статус

- ✅ Сайт "print-electrozavodskaya" существует в Firebase проекте
- ✅ Конфигурация `firebase.json` настроена правильно
- ✅ Сборка завершена успешно
- ⚠️ Нужно выполнить деплой

## 🚀 Выполнение деплоя

### Шаг 1: Убедитесь, что вы вошли в Firebase

```bash
firebase login
```

### Шаг 2: Переключитесь на правильный проект

```bash
firebase use d-print-electrozavodskaya
```

Если проект не найден, добавьте его:
```bash
firebase use --add
# Выберите проект d-print-electrozavodskaya
```

### Шаг 3: Убедитесь, что index.html существует

```bash
# Проверьте наличие файла
ls -la dist/index.html

# Если файла нет, создайте его
cp dist/index.firebase.html dist/index.html
```

### Шаг 4: Выполните деплой

```bash
firebase deploy --only hosting
```

Или используйте скрипт:
```bash
npm run deploy:russian
```

## 📋 Проверка конфигурации

Убедитесь, что `firebase.json` содержит:

```json
{
  "hosting": {
    "site": "print-electrozavodskaya",
    "public": "dist",
    ...
  }
}
```

## 🌐 После успешного деплоя

Приложение будет доступно по адресу:
- **https://print-electrozavodskaya.web.app**

## 🔧 Устранение проблем

### Ошибка: "Invalid project selection"

1. Проверьте список проектов:
   ```bash
   firebase projects:list
   ```

2. Если проекта нет, добавьте его:
   ```bash
   firebase use --add
   ```

3. Или используйте напрямую:
   ```bash
   firebase deploy --only hosting --project d-print-electrozavodskaya
   ```

### Ошибка: "Site not found"

Сайт "print-electrozavodskaya" уже существует в проекте. Просто выполните деплой.

### Ошибка: "Permission denied"

1. Войдите в Firebase:
   ```bash
   firebase login
   ```

2. Проверьте доступ к проекту в Firebase Console:
   https://console.firebase.google.com/project/d-print-electrozavodskaya

### Файлы не найдены в dist/

Пересоберите проект:
```bash
npm run build:firebase
```

## ✨ Готово!

После выполнения деплоя приложение будет доступно на https://print-electrozavodskaya.web.app

